<div class="modal fade" id="delete-department-modal-<%= index %>" tabindex="-1" aria-labelledby="delete-department-modal-label-<%= index %>" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5" id="delete-department-modal-label-<%= index %>">حذف قسم</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body fs-5">هل انت متأكد من حذف <strong><%= department.name %></strong>؟</div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">إغلاق</button>
        <a href="/departments/delete/<%= department._id %>" class="btn btn-danger">حذف</a>
      </div>
    </div>
  </div>
</div>