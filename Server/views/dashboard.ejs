<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard</title>
  <link rel="stylesheet" href="/styles/dashboard.css">
</head>

<body>
  <div class="dashboard-container">
    <header>
      <h1>Management Dashboard</h1>
      <nav>
        <a href="#worker">Workers</a>
        <a href="#department">Departments</a>
        <a href="#product">Products</a>
        <a href="#bonus">Bonuses</a>
        <a href="/auth/logout">Logout</a>
      </nav>
    </header>

    </div>

  <script>
    function editWorker(id) {
      // Fetch worker data by ID and show a modal for editing
      alert('Edit Worker ID: ' + id);
    }

    function editDepartment(id) {
      alert('Edit Department ID: ' + id);
    }

    function editProduct(id) {
      alert('Edit Product ID: ' + id);
    }

    function editBonus(id) {
      alert('Edit Bonus ID: ' + id);
    }
  </script>
</body>

</html>